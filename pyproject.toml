[project]
name = "streamlit-sql-lite"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "bcrypt>=5.0.0",
    "psycopg[binary]>=3.3.6",
    "streamlit>=1.62.0",
    "streamlit-authenticator>=0.4.2",
]
